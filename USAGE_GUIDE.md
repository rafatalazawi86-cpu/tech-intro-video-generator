# 📖 دليل الاستخدام - مولد فيديوهات Rafat Emad التقديمية

## جدول المحتويات
1. [التثبيت](#التثبيت)
2. [الاستخدام الأساسي](#الاستخدام-الأساسي)
3. [التخصيص المتقدم](#التخصيص-المتقدم)
4. [معالجة المشاكل](#معالجة-المشاكل)
5. [الأسئلة الشائعة](#الأسئلة-الشائعة)

---

## التثبيت

### الخطوة 1: تثبيت Python
تأكد من أن لديك Python 3.7 أو أحدث مثبت على جهازك.

### الخطوة 2: تثبيت المكتبات المطلوبة
```bash
pip install -r requirements.txt
```

أو يدويأً:
```bash
pip install opencv-python Pillow numpy
```

### الخطوة 3: إنشاء المجلدات
```bash
mkdir -p assets output
```

---

## الاستخدام الأساسي

### 1. ضع صورتك
```
📸 ضع صورتك في: assets/profile_pic.jpg
```

**متطلبات الصورة:**
- الحجم: 400×400 أو أكبر
- الصيغة: JPG أو PNG
- الجودة: عالية
- الخلفية: بسيطة

### 2. شغّل البرنامج
```bash
python rafat_intro_generator.py
```

### 3. انتظر النتيجة
```
الفيديو سيكون في: output/rafat_intro_video.mp4
```

---

## التخصيص المتقدم

### تعديل الاسم والعنوان

افتح ملف `rafat_intro_generator.py` وجد السطر:

```python
self.name = "Rafat Emad"         # اسمك
self.subtitle = "Digital Creator" # عنوانك
```

غيّرهما إلى ما تريد:
```python
self.name = "اسمك هنا"
self.subtitle = "تخصصك هنا"
```

### تغيير الألوان

عدّل القاموس `colors`:

```python
self.colors = {
    'circle_1': (255, 100, 150),   # BGR: Blue, Green, Red
    'circle_2': (100, 200, 255),
    'circle_3': (100, 255, 150),
    'circle_4': (255, 200, 100),
    'circle_5': (200, 100, 255),
    'circle_6': (255, 255, 100),
    'white': (255, 255, 255),
    'text_main': (255, 255, 255),
}
```

**ملاحظة:** OpenCV يستخدم BGR بدلاً من RGB، لذا الترتيب مختلف!

### تغيير أيقونات التواصل الاجتماعي

```python
self.social_media = [
    {'name': 'Instagram', 'icon': 'I', 'color': (100, 200, 255)},
    {'name': 'YouTube', 'icon': 'Y', 'color': (100, 100, 255)},
    {'name': 'منصتك', 'icon': 'M', 'color': (200, 100, 100)},
    # أضف المزيد هنا
]
```

### تغيير المدة والجودة

```python
self.fps = 30         # قلل من 60 إذا كان بطيئاً
self.duration = 10    # غيّر المدة بالثوانٍ
self.width = 1280     # قلل الدقة إذا لزم الأمر
self.height = 720
```

### تغيير سرعة الحركات

**سرعة دوران الدوائر:**
```python
angle = (2 * pi * i / num_circles) + (progress * 4 * pi)  # أسرع
```

**سرعة نبض الدوائر:**
```python
pulse = 30 * sin(progress * 6 * pi + i * pi / 3)  # أقوى
```

---

## معالجة المشاكل

### ❌ خطأ: "ModuleNotFoundError: No module named 'cv2'"

**الحل:**
```bash
pip install opencv-python
```

### ❌ خطأ: "صورة غير موجودة"

**تأكد من:**
1. وجود الملف في `assets/profile_pic.jpg`
2. أن الملف صورة صحيحة
3. أن لديك صلاحيات القراءة

**الحل البديل:**
سيستخدم البرنامج صورة زرقاء بديلة إذا لم يجد صورتك.

### ❌ خطأ: "الفيديو ينتج ببطء"

**الحلول:**
```python
self.fps = 30          # خفّض معدل الإطارات
self.duration = 5      # اقصّر المدة
self.width = 1280      # قلّل الدقة
self.height = 720
```

### ❌ المشكلة: "الألوان غريبة تماماً"

**التفسير:** OpenCV يستخدم BGR بدلاً من RGB

**الحل:** اعكس القيم. مثلاً:
- الأحمر الصحيح: (0, 0, 255) بدلاً من (255, 0, 0)
- الأخضر الصحيح: (0, 255, 0) بدلاً من (0, 255, 0) ✓
- الأزرق الصحيح: (255, 0, 0) بدلاً من (0, 0, 255)

### ❌ المشكلة: "الفيديو ملتقط بسيء"

**الحل:** تحسين الإضاءة في صورتك الأصلية قد يساعد.

---

## الأسئلة الشائعة

### س: هل يمكنني استخدام هذا تجارياً؟
**ج:** نعم! المشروع مفتوح المصدر.

### س: كيف أضيف موسيقى خلفية؟
**ج:** استخدم FFmpeg أو أداة مثل OpenShot:
```bash
ffmpeg -i output/rafat_intro_video.mp4 -i music.mp3 -c:v copy -c:a aac -shortest output/rafat_intro_with_music.mp4
```

### س: كيف أغيّر حجم الفيديو النهائي؟
```python
self.width = 1280
self.height = 720
```

### س: هل يمكنني استخدام فيديو بدلاً من صورة ثابتة؟
**ج:** حالياً لا، لكن يمكنك تعديل الكود لدعم هذه الميزة.

### س: كيف أسرّع الفيديو؟
```python
self.duration = 5      # اقصّر المدة
```

### س: هل الفيديو سيعمل على جميع المنصات؟
**ج:** نعم! تنسيق MP4 يعمل على YouTube, Instagram, TikTok, وغيرها.

### س: كيف أحسّن جودة الفيديو؟
```python
self.fps = 120         # زيادة الإطارات
# ولكن سيستغرق وقتاً أطول وحجماً أكبر
```

---

## نصائح وحيل

### نصيحة 1: اختبر تغييراتك
غيّر عنصراً واحداً فقط في كل مرة.

### نصيحة 2: احفظ نسخة احتياطية
```bash
cp rafat_intro_generator.py rafat_intro_generator_backup.py
```

### نصيحة 3: استخدم الدقة المنخفضة أولاً
لتختبر التأثيرات بسرعة.

### نصيحة 4: ادمج مع أدوات أخرى
استخدم HitFilm Express أو DaVinci Resolve لتحسينات إضافية.

---

## الدعم

إذا واجهت مشاكل:

1. تحقق من ملف [README.md](README.md)
2. اقرأ رسائل الخطأ بعناية
3. جرب الحل البديل المقترح
4. تواصل عبر البريد: rafatalazawi86@gmail.com

---

**استمتع بإنشاء فيديوهاتك!** 🎬✨
